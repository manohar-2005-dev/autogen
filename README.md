# autogen
Using AutoGen, this code sets up a basic agent conversation flow:  An AssistantAgent powered by an LLM A UserProxyAgent that initiates and controls the conversation An OpenAI-compatible configuration that connects AutoGen to Groq  Fully automated interaction with no human input required .
